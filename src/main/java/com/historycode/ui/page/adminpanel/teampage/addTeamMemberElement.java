package com.historycode.ui.page.adminpanel.teampage;

import com.historycode.ui.elements.BaseElement;
import org.openqa.selenium.WebDriver;

public class addTeamMemberElement extends BaseElement {
    public addTeamMemberElement(WebDriver driver) {
        super(driver);
    }
}
